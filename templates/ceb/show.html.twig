{% extends 'base.html.twig' %}

{% block title %}Ceb{% endblock %}

{% block body %}
    <h1>Ceb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ceb.id }}</td>
            </tr>
            <tr>
                <th>Nom_ceb</th>
                <td>{{ ceb.nomCeb }}</td>
            </tr>
            <tr>
                <th>Zone</th>
                <td>{{ ceb.zone }}</td>
            </tr>
            <tr>
                <th>Secteur</th>
                <td>{{ ceb.secteur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ceb_index') }}">back to list</a>

    <a href="{{ path('app_ceb_edit', {'id': ceb.id}) }}">edit</a>

    {{ include('ceb/_delete_form.html.twig') }}
{% endblock %}
