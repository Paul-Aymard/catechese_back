<form method="post" action="{{ path('app_ceb_delete', {'id': ceb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ceb.id) }}">
    <button class="btn">Delete</button>
</form>
